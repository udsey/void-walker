FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./

RUN pip install uv && uv sync --frozen

COPY dashboard/ dashboard/
COPY configs/ configs/

CMD ["uv", "run", "python", "-c", "from dashboard.app import app; app.run(debug=False)"]